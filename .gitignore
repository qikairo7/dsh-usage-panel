node_modules/
.npm-cache/
.tmp-npmcheck/
*.log
.DS_Store
.chrome-*/
.edge-*/
# Build output (regenerated by prepack on install). Anchored to the repo root
# on purpose: a bare `lib/` also matches src/lib/, which silently excluded the
# engine source from the published tree.
/lib/
# internal docs & evidence (not for the public repo)
SPEC.md
reports/
